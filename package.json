{
  "name": "saikyo-cw",
  "version": "0.3.0",
  "description": "All-in-one Chatwork enhancement Chrome extension",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build",
    "build": "tsc --noEmit && BUILD_TARGET=content vite build && BUILD_TARGET=background vite build && BUILD_TARGET=options vite build",
    "package": "npm run build && cd dist && zip -r ../saikyo-cw.zip .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "jsdom": "^29.0.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.2"
  },
  "license": "MIT"
}
